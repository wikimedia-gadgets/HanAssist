# Contributing
## Prerequisites
* `Node.js` v16
* `yarn`
* `typescript` >= 4.3.5
* Visual Studio Code (Recommended)

## Quick start
From the root of the repo, run:

```sh
yarn
yarn run lint
yarn run compile
```

Go to `./dist/` to see generated files.