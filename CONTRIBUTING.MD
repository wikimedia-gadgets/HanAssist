# Contributing
## Prerequisites
* `Node.js` v16
* `pnpm`
* `typescript` >= 4.3.5
* Visual Studio Code (Recommended)

## Quick start
At the root of the repo, run:

```sh
pnpm
pnpm run lint
pnpm run compile
```

View generated files at `./dist/`.
