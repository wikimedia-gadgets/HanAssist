# Contributing
## Prerequisites
* `Node.js`
* `npm` or `yarn`
* `typescript` >= 4.3.5
* Visual Studio Code (Recommended)

## Quick start
From the root of the repo, run:

```sh
npm install
npm run lint
npm run compile
```

Go to `./dist/` to see generated files.